# 2026-05-30 开发日志

## 桌宠生命感增强第一阶段

本轮先做低成本、高收益的生命感增强：继续使用当前 12 张透明 PNG 状态图，不新增素材，不引入骨骼动画，不新建数据库表，也不修改 `kaka-core` 协议。

新增能力：

```text
待机状态加入更轻的呼吸、浮动和小幅摆动。
开心、收到消息、思考、信号弱、炸毛、睡觉和拖拽状态使用不同节奏，减少所有状态动作都一样的机械感。
状态贴图切换时加入轻量弹入和淡入缓动，避免突然硬切。
桌宠大小仍跟随现有窗口大小设置，PixiJS 每帧按贴图高度计算基础缩放。
```

实现要点：

```text
apps/desktop-pet/src/PetCanvas.tsx
新增 MotionFrame，统一描述 x / y / rotation / scaleX / scaleY / alpha。
新增 stateChangedAtRef，记录状态切换时间，用于计算状态切换缓动。
将原本单一 scale 动作拆成横向/纵向缩放，支持轻微 squash/stretch。
新增 getStateTransitionFrame 和 easeOutCubic，给状态切换增加 280ms 的轻量弹入与淡入。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
npm run build passed
cargo check passed
cargo test passed（3 passed）
git diff --check passed（仅 CRLF 提示）
npm run tauri:build passed
release exe 已启动验证，当前 PID 29260
```

下一步建议：

```text
用户实测待机呼吸、状态切换、对话回复切状态、拖拽和睡觉动作是否自然。
如果第一阶段手感稳定，再考虑更高成本的动作方案，例如分层素材、Live2D、Spine 或逐帧动画。
```

## 桌宠生命感增强第二阶段

本轮继续做状态节奏和互动反馈联动。这个阶段只改桌宠主状态调度，不新增素材，不新建数据库表，也不修改 `kaka-core` 协议。

新增能力：

```text
给桌宠状态增加来源优先级：drag > conversation > system > chatReply > reaction > sleep/manual > ambient > idle。
拖拽、对话、系统检查、回复播放、普通互动、睡觉和随机待机不会随意互相覆盖。
旧 timer 恢复状态时会校验来源，避免把当前正在进行的高优先级状态切回待机。
对话回复成功后先短暂进入“收到消息”，再按内容进入思考/开心/收到消息，播放结束后轻停一下再回待机。
核心连接测试成功或失败后会自动恢复待机，避免长时间停在收到消息或信号弱。
普通摸头/长按不会打断正在播放的对话回复；拖拽中的长拖拽提示仍保留。
```

实现要点：

```text
apps/desktop-pet/src/App.tsx
新增 PetStateSource、PetStateOptions 和 PET_STATE_SOURCE_PRIORITY。
setPetState 改为带来源和优先级判断，低优先级状态不能覆盖高优先级状态。
对 chatReply、conversation、system、reaction、ambient、sleep、drag 的恢复逻辑分别绑定来源校验。
新增系统状态恢复 timer，连接测试结束后自动回 idle。
拖拽前状态只保留稳定手动状态，其余临时状态拖拽结束后回 idle。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
阶段一状态优先级整理：npm run build passed
阶段二互动和对话节奏增强：npm run build passed
最终 npm run build passed
cargo check passed
cargo test passed（3 passed）
git diff --check passed（仅 CRLF 提示）
npm run tauri:build passed
release exe 已启动验证，当前 PID 35452
```

下一步建议：

```text
用户实测待机、摸头、拖拽、长拖拽、连接测试、说话回复、失败重试和睡觉唤醒之间是否还会互相抢状态。
如果状态调度稳定，再考虑完整聊天历史窗口、快捷唤醒、语音入口或更高成本动画方案。
```

## 设置窗口连接测试同步

用户实测发现：从系统托盘执行 `连接测试` 时，卡咔本体会显示信号弱并自动回待机；但从设置窗口执行 `连接测试` 时，只有设置面板状态变化，卡咔本体没有反应。

修复内容：

```text
apps/desktop-pet/src/SettingsApp.tsx
设置窗口点击连接测试时，除调用 check_kaka_core_health 更新设置面板状态外，也 emit `kaka-tray-check-core` 事件给主窗口。
主窗口继续复用托盘连接测试已有逻辑，显示 loading / 核心信号正常 / 信号弱，并按状态调度规则自动恢复。
```

本轮没有新增素材，没有新建数据库表，也没有修改 `kaka-core` 协议。

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
npm run build passed
cargo check passed
cargo test passed（3 passed）
git diff --check passed（仅 CRLF 提示）
npm run tauri:build passed
release exe 已启动验证，当前 PID 40688
```

## 对话回复气泡播放修复

用户实测发现：卡咔对话后，最近对话里能看到回复，但卡咔本体上方没有气泡回复。

原因：

```text
回复成功后会先关闭输入面板，再排队播放回复气泡。
关闭输入面板会触发待机 effect 重排；旧 cleanup 同时清理了回复气泡播放 timer。
结果是回复已经进入最近对话，但刚排队的气泡播放被取消。
```

修复内容：

```text
apps/desktop-pet/src/App.tsx
将待机 timer / 随机待机 timer 的重排清理，和窗口卸载时的全量 timer 清理拆开。
普通状态变化只重排待机相关 timer，不再误清理 chatReplySequenceTimerRef。
```

本轮没有新增素材，没有新建数据库表，也没有修改 `kaka-core` 协议。

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
npm run build passed
cargo check passed
cargo test passed（3 passed）
git diff --check passed（仅 CRLF 提示）
首次 npm run tauri:build 因旧 release 进程占用 exe 失败，停止旧进程后重跑 passed
release exe 已启动验证，当前 PID 29608
用户实测剩余项无问题
```
