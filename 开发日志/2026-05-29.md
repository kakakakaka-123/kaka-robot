# 2026-05-29 开发日志

## 桌宠外观和行为设置增强

本轮在设置窗口补充外观和行为类选项。

新增设置：

```text
桌宠大小：小 / 标准 / 大
气泡停留时间：短 / 标准 / 长
随机待机频率：低 / 标准 / 高
桌宠始终置顶：开关
顶部状态标签：开关
```

实现要点：

```text
apps/desktop-pet/src/desktopPetSettings.ts
扩展桌宠设置结构、默认值和旧设置兼容读取。

apps/desktop-pet/src/SettingsApp.tsx
新增外观/行为控件，继续走 localStorage + Tauri 事件同步。

apps/desktop-pet/src/App.tsx
主桌宠监听设置变化后实时应用气泡时长、待机频率、状态标签显示和窗口外观。

apps/desktop-pet/src/PetCanvas.tsx
桌宠大小切换后，Pixi 渲染中的卡咔本体会随窗口尺寸等比缩放。

apps/desktop-pet/src-tauri/src/lib.rs
新增 set_main_window_size 和 set_main_window_always_on_top 命令。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
git diff --check 仅有 CRLF 提示
release exe 已启动验证
```

下一步建议：

```text
实测外观/行为设置是否实时生效。
实测托盘修复窗口和重启桌宠。
方便重启电脑时，再做开机自启真实重启回归测试。
```

## 桌宠互动行为增强第二阶段

本轮把桌宠本地互动从散落在 `App.tsx` 里的随机数组，整理为独立行为系统。

新增能力：

```text
单击摸头：继续触发摸头反应，但会按权重、冷却和连续摸头次数挑选反应。
双击：触发更强一点的在线/确认反馈。
长按：触发观察、害羞或轻微抗议类反馈。
长拖拽：拖拽超过一段时间后显示特殊气泡。
连续摸头：短时间内多次摸头会进入连续反应，后续可能开心，也可能轻微炸毛。
防重复：行为系统会尽量避免连续显示同一句气泡。
```

实现要点：

```text
apps/desktop-pet/src/petBehavior.ts
新增行为系统，统一管理状态气泡、反应权重、冷却、防重复、连续摸头计数和事件到反应的映射。

apps/desktop-pet/src/App.tsx
接入行为系统；新增单击延迟判定、双击识别、长按计时、长拖拽提示和统一的行为播放入口。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
git diff --check 仅有 CRLF 提示
release exe 已启动验证
```

下一步建议：

```text
用户实测单击、双击、长按、连续摸头和长拖拽手感。
方便重启电脑时，再做开机自启真实重启回归测试。
```
