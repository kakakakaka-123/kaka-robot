# 2026-05-29 开发日志

## 桌宠外观和行为设置增强

本轮在设置窗口补充外观和行为类选项。

新增设置：

```text
桌宠大小：小 / 标准 / 大
气泡停留时间：短 / 标准 / 长
随机待机频率：低 / 标准 / 高
桌宠始终置顶：开关
顶部状态标签：开关
```

实现要点：

```text
apps/desktop-pet/src/desktopPetSettings.ts
扩展桌宠设置结构、默认值和旧设置兼容读取。

apps/desktop-pet/src/SettingsApp.tsx
新增外观/行为控件，继续走 localStorage + Tauri 事件同步。

apps/desktop-pet/src/App.tsx
主桌宠监听设置变化后实时应用气泡时长、待机频率、状态标签显示和窗口外观。

apps/desktop-pet/src/PetCanvas.tsx
桌宠大小切换后，Pixi 渲染中的卡咔本体会随窗口尺寸等比缩放。

apps/desktop-pet/src-tauri/src/lib.rs
新增 set_main_window_size 和 set_main_window_always_on_top 命令。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
git diff --check 仅有 CRLF 提示
release exe 已启动验证
```

下一步建议：

```text
实测外观/行为设置是否实时生效。
实测托盘修复窗口和重启桌宠。
方便重启电脑时，再做开机自启真实重启回归测试。
```

## 桌宠互动行为增强第二阶段

本轮把桌宠本地互动从散落在 `App.tsx` 里的随机数组，整理为独立行为系统。

新增能力：

```text
单击摸头：继续触发摸头反应，但会按权重、冷却和连续摸头次数挑选反应。
双击：触发更强一点的在线/确认反馈。
长按：触发观察、害羞或轻微抗议类反馈。
长拖拽：拖拽超过一段时间后显示特殊气泡。
连续摸头：短时间内多次摸头会进入连续反应，后续可能开心，也可能轻微炸毛。
防重复：行为系统会尽量避免连续显示同一句气泡。
```

实现要点：

```text
apps/desktop-pet/src/petBehavior.ts
新增行为系统，统一管理状态气泡、反应权重、冷却、防重复、连续摸头计数和事件到反应的映射。

apps/desktop-pet/src/App.tsx
接入行为系统；新增单击延迟判定、双击识别、长按计时、长拖拽提示和统一的行为播放入口。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
git diff --check 仅有 CRLF 提示
release exe 已启动验证
```

下一步建议：

```text
用户实测单击、双击、长按、连续摸头和长拖拽手感。
方便重启电脑时，再做开机自启真实重启回归测试。
```

## 桌宠可靠性验收收尾

本轮把用户对互动行为增强第二阶段的实测结果同步到文档，并对托盘维护入口做自动化可覆盖部分的验收。

已确认：

```text
用户实测单击、双击、长按、连续摸头和长拖拽手感：无问题。
Windows UI Automation 可定位真实托盘图标 `卡咔桌宠`。
托盘图标左键显示/隐藏链路可触发。
重启逻辑已做等价进程级验证：启动当前 release exe 的新进程后关闭旧进程，新进程正常运行。
当前 release 实例 PID：27584。
```

验收边界：

```text
Windows 11 隐藏托盘右键菜单是原生 #32768 菜单，自动化读取菜单项文字和稳定点选不可靠。
本轮没有把“自动脚本点中修复窗口/重启桌宠菜单项”记为已通过。
后续仍建议用户手动右键托盘，点一次“修复窗口”和“重启桌宠”做最终确认。
```

下一步建议：

```text
用户手动最终确认托盘右键菜单的“修复窗口”和“重启桌宠”。
方便重启电脑时，再做开机自启真实重启回归测试。
```

## 桌宠轻量输入入口和最小核心对话接入

本轮完成桌宠对话入口的第二步和第三步：先做输入入口骨架，再接入 `kaka-core /v1/chat` 最小链路。

新增能力：

```text
右键菜单新增“说话”入口。
点击后打开小输入框，支持发送、取消和 Esc 关闭。
输入框限制 80 字，打开或发送中时不会被随机待机和闲置睡觉打断。
发送时切到 loading 状态，并显示“卡咔思考中...”。
Rust 后端组装 platform=desktop 的 MessageEvent，请求 http://127.0.0.1:8001/v1/chat。
从 KakaResponse.actions 中读取第一条 send_text 文本，并显示到桌宠气泡。
请求失败时切到 weakSignal 状态，保留输入内容方便重试。
当前只做气泡回复，不做完整聊天历史窗口。
```

实现要点：

```text
apps/desktop-pet/src/App.tsx
新增 conversationPanelVisible / conversationInput / conversationPending 状态，右键菜单接入“说话”，输入框发送后调用 Tauri 命令。

apps/desktop-pet/src/styles.css
新增轻量输入框样式，使用高 z-index 避免被拖拽层拦截。

apps/desktop-pet/src-tauri/src/lib.rs
新增 send_desktop_chat_message 命令。命令使用 spawn_blocking 执行本地 TCP HTTP 请求，避免长时间模型回复阻塞前端；并补充响应解析单元测试。
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
阶段二输入入口骨架：npm run build passed；cargo check passed。
阶段三最小核心对话链路：npm run build passed；cargo check passed；cargo test passed（3 passed）。
最终 Tauri release 构建：npm run tauri:build passed。
release exe 已启动验证，当前 PID 1284。
kaka-core /health 探测：当前无法连接 127.0.0.1:8001，因此未主动请求 /v1/chat，避免消耗真实模型调用。
```

下一步建议：

```text
用户启动 kaka-core 后，手动从桌宠右键菜单打开“说话”，测试真实回复。
观察气泡长度、发送中状态、失败重试和状态恢复是否自然。
如果真实回复链路手感没问题，再考虑聊天历史、快捷唤醒、快捷键或语音入口。
```
