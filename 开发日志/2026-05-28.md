# 2026-05-28 开发日志

今天继续打磨 `apps/desktop-pet` 的常驻桌宠体验。

## 托盘菜单状态优化

本轮解决托盘菜单状态表达不清的问题：

```text
卡咔显示时：托盘菜单显示“隐藏卡咔”
卡咔隐藏时：托盘菜单显示“显示卡咔”
```

同时补上开机自启状态同步：

```text
设置窗口读取开机自启状态
-> 同步刷新托盘菜单里的“开机自启”勾选状态

设置窗口切换开机自启
-> Rust/Tauri 侧注册或取消系统启动项
-> 同步刷新托盘菜单勾选状态
```

实现位置：

```text
apps/desktop-pet/src-tauri/src/lib.rs
```

## 验证

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
```

备注：

```text
git diff --check 只有 CRLF 提示，这是当前仓库里的常见换行提示，不是代码失败。
```

用户已实测：

```text
托盘菜单显示/隐藏文字同步正常。
设置窗口和托盘开机自启状态同步暂无问题。
```

## 设置窗口细节打磨

本轮继续打磨设置窗口：

```text
状态值改为颜色标签
开机自启 / 连接测试 / 重置位置 / 自启显示设置执行时显示处理中状态
操作执行中临时禁用按钮和控件，避免重复点击
成功、失败、检查中提示使用不同样式
```

实现位置：

```text
apps/desktop-pet/src/SettingsApp.tsx
apps/desktop-pet/src/styles.css
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build
```

结果：

```text
passed
```

## 设置窗口主窗口控制

用户暂时不能做关机/重启测试，因此本轮先继续做不依赖重启的常驻体验。

新增设置窗口里的桌宠窗口控制：

```text
状态区显示桌宠窗口当前是“显示中”还是“已隐藏”
设置窗口可直接显示/隐藏卡咔主窗口
设置窗口可直接退出卡咔
```

Rust/Tauri 侧新增命令：

```text
get_main_window_visible
set_main_window_visible
```

实现位置：

```text
apps/desktop-pet/src-tauri/src/lib.rs
apps/desktop-pet/src/SettingsApp.tsx
apps/desktop-pet/src/styles.css
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
```

## 右键状态菜单收敛

本轮继续把桌宠本体从调试面板收敛成正式桌宠入口。

右键菜单默认只保留：

```text
摸摸头
设置
隐藏
退出
```

设置窗口新增开关：

```text
启用右键调试状态菜单
```

该开关默认关闭。打开后，右键菜单才显示：

```text
连接测试
重置位置
12 个状态切换按钮
```

实现位置：

```text
apps/desktop-pet/src/App.tsx
apps/desktop-pet/src/SettingsApp.tsx
apps/desktop-pet/src/desktopPetSettings.ts
apps/desktop-pet/src/styles.css
apps/desktop-pet/src-tauri/src/lib.rs
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2
git diff --check

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
release exe 已启动验证
```

## 设置窗口白屏兜底修复

用户反馈打开设置面板时出现卡住、窗口空白。

本轮先做两个稳妥修复：

```text
设置窗口 URL 改回 index.html
前端通过 Tauri 窗口 label 识别 settings 并渲染设置页
前端入口增加 ErrorBoundary，运行时异常显示“窗口加载失败”而不是纯空白
设置页允许纵向滚动，避免后续内容高度变化被截断
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
cargo test

cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run tauri:build
```

结果：

```text
passed
release exe 已启动验证
```

## 下一步

继续体验优先路线：

```text
右键菜单收敛实测
设置窗口新增操作实测
开机自启真实重启回归测试
```

当前仍不急着接入桌宠到 `kaka-core /v1/chat` 的完整对话能力。
