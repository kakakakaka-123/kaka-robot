# 2026-05-03 开发日志

## 今日检查

今天的工作重点是把长期记忆这条线继续收口：自动候选区复核接进程序，正式记忆管理脚本补齐，并把文档同步到当前状态。

## 自动候选区复核

新增程序内置的候选区复核调度器：

```text
services/kaka-core/src/kaka_core/memory/auto_review.py
services/kaka-core/src/kaka_core/api/app.py
```

当前策略：

```text
每个整点检查一次
pending >= 20 才触发
每批 10 条
每次检查最多 1 批
```

复核逻辑复用 `review_memory_candidates.py` 的核心函数，避免重复实现一套新流程。

## 正式记忆管理

新增正式记忆管理脚本：

```text
services/kaka-core/scripts/manage_memories.py
```

支持：

```text
active / archived 切换
确认错误、垃圾、敏感时硬删除
默认先预览，`--apply` 才写库
硬删除要求额外 `--yes`
```

日常建议：

```text
先归档，确认垃圾再删
```

## 代码整理

顺手修了一个命令行参数边界问题：

```text
manage_memories.py 的 parse_args() 之前会忽略真实命令行参数
现在已修正，并补了回归测试
```

同时把自动分析脚本和记忆管理脚本的说明补得更明确了一些。
`manage_memories.py` 也补上了和其他辅助脚本一致的 PyCharm 入口参数说明；由于它可能修改数据库，默认参数保持为空，避免误操作。

## 文档同步

今天同步了这些文档：

```text
README.md
KAKA_HANDOFF.md
docs/运行手册.md
docs/开发运行说明.md
docs/下次上下文.md
docs/路线图.md
docs/长期记忆设计.md
scripts/doctor.py
.env.example
```

重点补充了：

```text
自动候选区复核
正式记忆管理脚本
当前测试结果
当前阶段状态
对接文档中的最新项目边界
```

最后整理了 `KAKA_HANDOFF.md` 底部的重复编号、数据观察脚本清单和下一步成功标准。

## 前半段测试结果

```text
kaka-core：61 passed
qq-adapter：18 passed
kaka-protocol：5 passed
doctor.py：58 OK，0 WARN，0 FAIL
compileall：通过
```

## Web 管理台和脚本收口

后半段继续把“用户日常跑脚本管理数据”的路线改成“网页管理为主，脚本留给开发和应急”。

当前 Web 管理台入口：

```text
http://127.0.0.1:8001/admin
```

前端和后端位置：

```text
apps/web-console
services/kaka-core/src/kaka_core/api/admin_routes.py
services/kaka-core/src/kaka_core/admin/service.py
```

当前覆盖：

```text
总览
最近对话
输入分析预览和状态调整
候选区列表和状态调整
候选合并预览/执行
正式记忆归档/恢复/删除
正式记忆检索预览
系统状态
```

脚本定位已经调整为：

```text
开发
测试
批量修复
应急排查
```

不再把脚本作为用户日常数据管理的主入口。

## Web 状态调整修复

修复并验证了两个容易出问题的写库入口：

```text
POST /admin/api/inputs/status
POST /admin/api/candidates/status
```

输入分析页现在可以筛选和调整：

```text
not_analyzed
analyzed
skipped
```

候选区页现在可以筛选和调整：

```text
pending
approved
rejected
merged_duplicate
```

候选合并预览和执行按钮只在 `pending` 视图下显示，避免误操作已经处理过的候选。

## 真实数据库验证

在真实数据库上测试前先做了备份：

```text
data/kaka.sqlite3.backup-webfix-20260503-2255
```

已验证写库操作：

```text
inputs.id=796 -> analysis_status=skipped
memory_candidates.id=42 -> approved -> rejected -> approved
```

相关截图：

```text
data/admin-8001-status-fix.png
data/admin-real-inputs.png
data/admin-real-candidates.png
```

## 后半段验证结果

```text
apps/web-console: npm run build passed
services/kaka-core: pytest tests/test_admin_api.py tests/test_merge_memory_candidates.py tests/test_analyze_inputs.py -> 31 passed
services/kaka-core: pytest tests/test_admin_api.py -> 4 passed
真实数据库状态写入验证：通过
```

没有在后半段重新跑完整 `kaka-core`、`qq-adapter` 和 `kaka-protocol` 全量测试；文档中的完整测试数量保留为历史记录，并单独标注。

## 文档收尾

今天结束前同步了项目 Markdown 文档：

```text
README.md
KAKA_HANDOFF.md
docs/下次上下文.md
docs/开发运行说明.md
docs/运行手册.md
docs/长期记忆设计.md
docs/路线图.md
docs/技术栈说明.md
docs/协议说明.md
docs/QQ适配器说明.md
卡咔电子生命系统设计文档.md
services/kaka-core/scripts/README.md
开发日志/2026-05-03.md
```

重点同步：

```text
/admin 已成为日常数据管理入口
脚本改为开发、测试、批量修复和应急备用
输入分析状态语义
候选区状态语义
正式记忆状态语义
Web 管理台真实库验证结果
下一步优先做网页端手动验收和真实 QQ 链路观察
```

## 下一步

接下来主要是保持真实 QQ 链路运行，观察：

```text
自动候选分析是否稳定
自动候选区复核是否稳定
回复中的长期记忆引用是否自然
Web 管理台管理输入、候选和正式记忆是否顺手
不合适的 memories 是否需要归档或删除
```

当前阶段不要再急着加新功能，先把现有链路跑稳。
