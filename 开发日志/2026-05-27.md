# 2026-05-27 开发日志

## 本日目标

今天目标是启动卡咔桌宠方向，先完成桌面身体原型，而不是直接接复杂对话系统。

本日选择的桌宠技术组合：

```text
Tauri 2 + React/Vite + TypeScript + PixiJS
```

设计判断：

- Tauri 负责透明桌面窗口、置顶、无边框和原生窗口位置。
- React / TypeScript 负责交互状态、菜单和气泡。
- PixiJS 负责透明 PNG 贴图渲染和轻量状态动作。
- 当前先不接 `kaka-core`，先把桌宠身体、状态和基础互动做好。

## 已完成

新增桌宠工程：

```text
apps/desktop-pet
```

当前已完成：

- Tauri 透明、无边框、置顶窗口。
- React/Vite/TypeScript 前端工程。
- PixiJS 桌宠贴图渲染。
- 12 个透明 PNG 状态素材接入：
  - 待机
  - 开心
  - 困困
  - 思考
  - 炸毛
  - 装死404
  - 收到消息
  - 睡觉
  - 摸头反应
  - 拖拽反应
  - 加载中
  - 信号弱
- 右键菜单切换状态和退出。
- 菜单位置和尺寸已修正，避免在 `280x280` 窗口里被裁切。
- 左键轻点触发摸头反应，约 2 秒后回到待机。
- 左键拖拽移动窗口，拖拽时显示拖拽反应，松手后恢复拖拽前状态。
- 2 分钟闲置后自动睡觉。
- 睡觉后点击、拖动或右键会唤醒。
- 消息气泡雏形：
  - 摸头时显示短句。
  - 闲置睡觉时显示短句。
  - 睡醒时显示短句。
  - 手动切状态时显示对应短句。

## 关键修正

### 右键菜单裁切

最初右键菜单高度接近 `280x280` 窗口高度，底部“退出”会被窗口裁掉。

处理方式：

- 缩小菜单宽度、行高和间距。
- 根据窗口尺寸限制菜单坐标。
- 菜单保持两列，作为当前开发调试入口。

### 右键菜单遮挡卡咔

当前右键菜单会遮挡卡咔本体，这是现阶段可接受的问题。

原因：

- 窗口只有 `280x280`。
- 菜单必须画在透明窗口内部。
- 如果直接放大透明窗口，会增加桌面误点区域。

后续正式版更适合改成：

- 系统托盘菜单。
- 单独设置窗口。
- 更轻量的右键控制入口。

### 拖拽状态恢复

第一版使用 Tauri 原生 `startDragging()`，Web 侧有时收不到拖拽结束事件，导致拖拽状态恢复不稳定。

后续改为自定义拖拽：

- 读取光标位置。
- 读取窗口位置。
- 根据光标位移设置窗口位置。
- 拖拽状态由 React 自己控制，松手后恢复拖拽前状态。

Tauri 权限相应改为：

```text
core:window:allow-cursor-position
core:window:allow-outer-position
core:window:allow-set-position
```

## 验证结果

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet
npm run build
```

结果：

```text
passed
```

已执行：

```powershell
cd D:\Python\AgentRobot\kaka-v2\apps\desktop-pet\src-tauri
cargo check
```

结果：

```text
passed
```

曾尝试执行：

```powershell
npm run tauri -- build --debug
```

失败原因：

```text
Windows 拒绝覆盖正在运行的 kaka-desktop-pet.exe
```

这是因为桌宠程序仍在运行，不是代码错误。关闭桌宠后可以重新打 debug 包。

## 文档同步

本轮同步更新：

```text
README.md
KAKA_HANDOFF.md
docs/下次上下文.md
docs/开发运行说明.md
docs/技术栈说明.md
docs/桌宠开发说明.md
开发日志/2026-05-27.md
```

## 下一步建议

下一步建议做桌宠到 `kaka-core` 的最小接入：

```text
桌宠固定输入或小输入框
-> 组装 platform=desktop 的 MessageEvent
-> POST http://127.0.0.1:8001/v1/chat
-> 把 KakaResponse 文本显示到气泡
-> 请求中显示加载中
-> 收到回复显示收到消息或开心
-> 请求失败显示信号弱
```

先不要急着做完整聊天窗口、系统托盘、复杂设置页或主动行为。当前优先把“桌宠身体 + 核心大脑回复 + 气泡表达”的最小闭环跑通。
